export interface ServicesDict {
  meta: { title: string; description: string };
  hero: {
    backgroundText: string;
    badgeItems: string[];
    watermark: string;
    tickerText: string;
    legend: string;
    title: string;
    description: string;
  };
}
